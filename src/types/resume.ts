type Education = {
  title: string;
  description: string;
  startDate: string;
  endDate: string;
};

type Experience = {
  title: string;
  description: string;
  bulletPoints: string[];
};

type ResumeProject = {
  title: string;
  link: string;
  description: string;
  bulletPoints: string[];
};

type Skill = {
  name: string;
  items: string[];
};

type Certification = {
  label: string;
  href: string;
};

